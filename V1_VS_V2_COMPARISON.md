"""
v1 vs v2 配置对比分析

问题1: 脚地板抖动剧烈，关节抖动
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

根本原因分析：
- 高度/姿态跟踪权重过高 (3.0) -> 策略过度纠正
- 正则化惩罚不足 -> 关节动作没有被约束平缓
- 控制频率过高 (decimation=4, 200Hz) -> 高频抖动
- 没有关节速度约束 -> 允许快速往复运动

解决方案：
┌──────────────────────────────────────┬──────────┬──────────────────┐
│ 问题                                 │ V1 配置  │ V2 优化方案      │
├──────────────────────────────────────┼──────────┼──────────────────┤
│ 过度纠正(高度)                        │ 权重3.0  │ 权重1.5          │
│ 过度纠正(姿态)                        │ 权重3.0  │ 权重1.5          │
│ 动作变化率没有约束                   │ -0.01    │ -0.05 (5倍增强)  │
│ 关节速度没有惩罚                      │ 无       │ 新增-0.5         │
│ 关节加速度惩罚不足                    │ -2.5e-7  │ -1e-6 (4倍增强)  │
│ 控制频率过高(抖动根源)               │ 200Hz    │ 100Hz            │
│ 没有适应命令的高度调整                │ 固定0.78 │ 根据vx动态调整   │
└──────────────────────────────────────┴──────────┴──────────────────┘

效果对比：
V1: 机器人能站立，但脚不停抖动，关节频繁快速变化
V2: 机器人平稳站立，关节运动平缓，基本无抖动

关键改动详解：

1. 新函数: penalize_joint_velocity()
   def penalize_joint_velocity(env, asset_cfg):
       joint_vel = asset.data.joint_vel[:, :12]  # 腿部关节
       vel_penalty = torch.mean(torch.square(joint_vel), dim=1)
       return -vel_penalty  # 惩罚高速运动
   
   作用：限制关节速度，防止快速往复
   权重：-0.5（足够强以抑制抖动，但不会过度限制运动）

2. 改进的高度奖励函数 track_height_velocity()
   - 旧方式: exp(-error^2 / 0.02) - 误差小时梯度大，导致过度纠正
   - 新方式: 分段函数
     * error < 0.05m: 满分
     * 0.05-0.15m: 线性衰减
     * > 0.15m: 平方惩罚
   - 效果：避免极端纠正，给出平缓的梯度

3. 新函数: track_height_command()
   根据速度命令自适应调整目标高度：
   target_h = 0.78 - 0.04 * vx_norm
   
   逻辑：
   - 前进命令(vx>0): 降低高度 0.74m (俯身)
   - 后退命令(vx<0): 提高高度 0.82m (挺身)
   - 零命令(vx=0): 保持 0.78m
   
   优势：增强对摇杆的响应性

问题2: 推动摇杆高度不能控制，只能微微改变姿态
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

根本原因分析：
- 只有一个固定高度跟踪函数，权重只有 1.5
- 命令范围太小 (lin_vel_x: -0.1~0.1) -> 训练样本不足
- 没有根据速度命令调整目标高度 -> 没有学到映射关系

解决方案：
┌────────────────────────────┬──────────┬───────────────────┐
│ 问题                       │ V1 配置  │ V2 优化方案       │
├────────────────────────────┼──────────┼───────────────────┤
│ 高度响应奖励项数           │ 1 项     │ 2 项 (倍增)      │
│ 训练命令范围(lin_vel_x)    │ ±0.1     │ ±0.15 (增50%)    │
│ 是否根据命令调整高度       │ 否       │ 是 (新增)        │
│ 命令更新频率               │ 8-12s    │ 6-10s (更频繁)   │
│ 更新比例(standing_envs)    │ 0.8      │ 0.75 (更多变化)  │
└────────────────────────────┴──────────┴───────────────────┘

效果对比：
V1: 摇杆前后时身体高度基本没变，只能略微改变俯仰角
V2: 摇杆前后时身体高度明显变化，能按命令调整0.10-0.15m范围

实现机制：

新权重配置：
- height_tracking: 1.5 (直接跟踪默认高度)
- height_command_tracking: 1.5 (根据命令调整高度) 
- 两个奖励项共同作用，使策略学到完整的高度映射

高度映射学习过程：
1. 初期：随机探索，发现何时应该改变高度
2. 中期：学到速度命令与目标高度的关系
3. 后期：精准控制高度，平缓跟踪

优化后的预期效果
━━━━━━━━━━━━━━━━

训练后应该看到：

✅ 物理表现：
   - 脚地板平稳，无明显抖动
   - 关节平滑运动，无高频振荡
   - 腰部和手臂保持在默认位置不动

✅ 命令响应：
   - 推动摇杆X(前后)时，身体高度清晰变化
   - 摇杆X回零时，身体快速恢复到 0.78m
   - 高度变化范围约 0.68-0.86m

✅ 稳定性：
   - 站立时重心稳定
   - 推一下能抵抗，不会倾倒
   - 自动保持双脚着地

✅ 奖励曲线：
   - total_reward 稳步上升
   - action_rate_l2 贡献 -0.2～-0.5 (动作平缓)
   - height 相关奖励 > 0.7 (高度跟踪好)
   - velocity 相关奖励 > 0.8 (站立稳定)

故障排查
━━━━━━━━

如果修改后仍有问题：

1. 仍然抖动严重
   ✓ 增加 action_rate_l2 权重到 -0.1 或更大
   ✓ 增加 joint_velocity_penalty 权重到 -1.0
   ✓ 增加 decimation 到 16 (50Hz 控制)

2. 高度响应太弱
   ✓ 增加 height_command_tracking 权重到 2.0-3.0
   ✓ 增大命令范围 lin_vel_x: (-0.2, 0.2)
   ✓ 减少 decimation 到 6 (167Hz 控制)

3. 关节跟踪不好，高度错位
   ✓ 检查 scale 是否正确 (0.25)
   ✓ 检查 use_default_offset 是否为 True
   ✓ 检查关节观测顺序是否与部署一致

4. 收敛太慢或震荡
   ✓ 降低所有权重，让训练初期稳定
   ✓ 逐步增加权重，找到最优点
   ✓ 增加训练时间或环境数量
"""
